const rootNode = document.getElementById("app");    // элемент для рендеринга приложения React

const root = ReactDOM.createRoot(rootNode);// получаем корневой элемент 

root.render();// рендеринг в корневой элемент